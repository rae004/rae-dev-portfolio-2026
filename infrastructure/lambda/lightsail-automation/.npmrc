# Supply chain hardening — see documentation/supply_chain_hardening.md
#
# Lambda deps are @aws-sdk/* + node-fetch — no native modules, no legitimate
# postinstall need. Block all install scripts as a containment layer.
#
# Cooldown is enforced via Renovate (.github/renovate.json).

ignore-scripts=true
fund=false
